import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cabaniaConnect',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
